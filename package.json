{
  "name": "autostay-weather-ops-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "AUTOSTAY Weather Ops Pack dashboard for operations, recovery, and marketing actions.",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "test": "node --test tests/dashboard.test.js",
    "check": "node --check app.js && node --check middleware.js && node --check api/auth.js && node --check api/check.js && node --check api/weather-ops-data.js && node --test tests/dashboard.test.js"
  }
}
